@model IEnumerable<FrontEnd.Models.DesparasitacionesVacunaViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CodigoDesparasitacionVacuna)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Producto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MascotaId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CodigoDesparasitacionVacuna)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Producto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MascotaId)
            </td>
            <td>
                    @Html.ActionLink("Edit", "Edit", new { id=item.CodigoDesparasitacionVacuna }) |
                    @Html.ActionLink("Details", "Details", new { id=item.CodigoDesparasitacionVacuna }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.CodigoDesparasitacionVacuna })
            </td>
        </tr>
}
    </tbody>
</table>
